# Dev-only image: runs the backend through air. Not for production
FROM golang:1.25-alpine

WORKDIR /app

# air is installed into /go/bin which is already on PATH in this image
RUN go install github.com/air-verse/air@latest

COPY go.mod go.sum ./
RUN go mod download

CMD ["air"]
